export enum ROUTE {
    MAIN = 'main',
    LOGIN = 'login',
    REGISTRATION = 'registration',
    CATALOG = 'catalog',
    PRODUCT = 'product',
    USER = 'user',
    BASKET = 'basket',
    ABOUT = 'about',
    NOT_FOUND = 'not-found',
}
